/*import testimonialList from '../JSON/testimonialList.js';

function createTestimonialItems(){

    var TestimonialList = testimonialList.map((TestimonialItem)=>{
        var testimonialElement = `
        <ul class = "testimonialCarouselTestimonial">
            <li class = "testimonialCarouselItem" > `+TestimonialItem.testimonial +`</li>
            <li class = "testimonialCarouselName">`+ TestimonialItem.name +`</li>
        </ul>
        `
        return testimonialElement

    })

    return TestimonialList.join("")
}


export default createTestimonialItems;*/
